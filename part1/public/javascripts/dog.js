// vue script for fetching image

const { createApp } = Vue;

createApp({

    data() {
        return {
            imageSrc: ''
        };
    },

    methods: {
        fetchDogImage() {
            try {
                response = 
            } catch (error) {
                console.error(error);
            }
        }
    }

})