// vue script for fetching image

const { createApp } = Vue;

createApp({

    data() {
        return {
            imageSrc: ''
        };
    },

    methods: {
        fetchDogImage() {
            try {

            } catch (error) {
                console.error(error);
            }
        }
    }

})